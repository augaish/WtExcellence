image: atandrau/way_to_excellence

<% require "dotenv"; Dotenv.load(".env") %>

registry:
  username: atandrau
  password:
    - KAMAL_REGISTRY_PASSWORD

service: way_to_excellence

builder:
  arch: amd64

servers:
  web:
    hosts:
      - 8.213.80.159
    options:
      memory: "4g"

  job:
    hosts:
      - 8.213.80.159
    cmd: bundle exec sidekiq -q default -c 5
    options:
      memory: "4g"
  
  cron_small:
    hosts:
      - 8.213.80.159
    cmd: bundle exec sidekiq -q cron_small -c 5
    options:
      memory: "4g"


proxy:
  app_port: 3000
  healthcheck: # Configure a custom healthcheck (default is /up on port 3000)
    path: /up
    interval: 20

boot:
  limit: 10 # Can also specify as a percentage of total hosts, such as "25%"
  wait: 2

env:
  clear:
    RAILS_ENV: production
    RAILS_LOG_TO_STDOUT: true
    NEW_RELIC_LOG: "stdout"
    PRODUCT: "way_to_excellence"
    RAILS_SERVE_STATIC_FILES: "true"
    ROOT_URL: "https://app.wtexcel.com"
  secret:
    - RAILS_MASTER_KEY
    - SECRET_KEY_BASE
    - POSTGRES_PASSWORD
    - REDIS_PASSWORD
    - DATABASE_URL
    - REDIS_URL
    - OPENROUTER_API_KEY
    - OPENROUTER_MODEL
    - OLLAMA_URL
    - CAPA_ACTION_PROVIDER
    - CAPA_CLAUSE_PROVIDER
    - CAPA_QUESTIONNAIRE_PROVIDER
    - CAPA_ACTION_OLLAMA_MODEL
    - CAPA_CLAUSE_OLLAMA_MODEL
    - CAPA_QUESTIONNAIRE_OLLAMA_MODEL
    - CAPA_QUESTIONNAIRE_SINGLE_PAIR_OLLAMA_MODEL
    - CAPA_ROOT_CAUSE_OLLAMA_MODEL
    - ALIYUN_ACCESS_KEY_ID
    - ALIYUN_ACCESS_KEY_SECRET
    - ALIYUN_BUCKET
    - ALIYUN_REGION
    - ALIYUN_ENDPOINT
    - SMTP_PORT
    - SMTP_SERVER
    - SMTP_LOGIN
    - SMTP_PASSWORD
    - SMTP_DOMAIN
    - SENTRY_DSN


proxy:
  ssl: true
  hosts:
    - app.wtexcel.com
    - www.wtexcel.com
    - wtexcel.com
  response_timeout: 120
  app_port: 3000
  healthcheck: # Configure a custom healthcheck (default is /up on port 3000)
    path: /up
    interval: 20

accessories:
  db:
    image: postgres:17
    host: 8.213.80.159
    port: 5432
    env:
      clear:
        POSTGRES_USER: 'way_to_excellence'
        POSTGRES_DB: 'way_to_excellence_prod'
      secret:
        - POSTGRES_PASSWORD
    directories:
      - data:/var/lib/postgresql/data
  redis:
    image: redis:7.0
    host: 8.213.80.159
    cmd: redis-server --requirepass <%= ENV['PROD_REDIS_PASSWORD'] %>
    port: 6379
    directories:
      - data:/data
