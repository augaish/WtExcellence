<%= content_for :title, @risk_workspace.name %>

<div class="min-h-screen bg-gray-50">
  <div class="max-w-7xl mx-auto px-6 py-8">
    <div class="flex items-center justify-between mb-8">
      <div>
        <%= link_to t('back_to_workspaces'), dashboard_risk_workspaces_path, class: "text-sm text-[#5C3984] hover:underline mb-2 inline-block" %>
        <h1 class="text-3xl font-bold text-[#0D1120] mb-1"><%= @risk_workspace.name %></h1>
        <% if @risk_workspace.framework.present? %>
          <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-[#F6EEFF] text-[#5C3984]">
            <%= @risk_workspace.framework %>
          </span>
        <% end %>
      </div>
      <%= link_to t('edit'), edit_dashboard_risk_workspace_path(@risk_workspace), class: ui_btn_secondary %>
    </div>

    <% if @risk_workspace.description.present? %>
      <p class="text-gray-600 mb-6"><%= @risk_workspace.description %></p>
    <% end %>

    <div class="bg-white border border-[#E3E3E3] rounded-xl overflow-hidden">
      <div class="p-4 border-b border-[#E3E3E3]">
        <h2 class="text-lg font-semibold text-[#0D1120]"><%= t('risks') %></h2>
      </div>
      <div class="overflow-x-auto">
        <table class="w-full">
          <thead class="bg-[#F7F7FD] border-b border-[#E3E3E3]">
            <tr>
              <th class="text-left p-4"><span class="text-xs font-semibold text-[#797C81]">Title</span></th>
              <th class="text-left p-4"><span class="text-xs font-semibold text-[#797C81]">Owner</span></th>
              <th class="text-left p-4"><span class="text-xs font-semibold text-[#797C81]">Inherent Score</span></th>
              <th class="text-left p-4"><span class="text-xs font-semibold text-[#797C81]">Status</span></th>
              <th class="text-left p-4"><span class="text-xs font-semibold text-[#797C81]">Action</span></th>
            </tr>
          </thead>
          <tbody>
            <% if @risks.any? %>
              <% @risks.each do |risk| %>
                <tr class="border-b border-[#E3E3E3] hover:bg-[#F7F7FD]">
                  <td class="p-4 text-sm font-medium text-[#0D1120]"><%= risk.title %></td>
                  <td class="p-4 text-sm text-[#797C81]"><%= risk.owner&.user&.name || "Unassigned" %></td>
                  <td class="p-4">
                    <span class="px-2 py-1 text-xs font-medium rounded-full <%= risk.inherent_level == 'low' ? 'bg-green-100 text-green-800' : risk.inherent_level == 'medium' ? 'bg-yellow-100 text-yellow-800' : risk.inherent_level == 'high' ? 'bg-orange-100 text-orange-800' : 'bg-red-100 text-red-800' %>">
                      <%= risk.inherent_score %> (<%= risk.inherent_level %>)
                    </span>
                  </td>
                  <td class="p-4 text-sm text-[#797C81]"><%= risk.status.titleize %></td>
                  <td class="p-4">
                    <%= link_to t('view'), dashboard_risk_management_path(risk), class: "text-blue-500 hover:text-blue-700 text-sm" %>
                  </td>
                </tr>
              <% end %>
            <% else %>
              <tr>
                <td colspan="5" class="p-8 text-center text-sm text-[#797C81]"><%= t('no_risks_in_workspace') %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
