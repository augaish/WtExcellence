<%= content_for :title, "Log New Risk" %>

<div class="min-h-screen bg-gray-50">
  <div class="max-w-3xl mx-auto px-6 py-8">
    <h1 class="text-2xl font-bold text-[#0D1120] mb-6"><%= t('log_new_risk') %></h1>
    <%= render "form" %>
  </div>
</div>
