<%= content_for :title, t('edit_commitment') %>

<div class="min-h-screen bg-gray-50">
  <div class="max-w-2xl mx-auto px-6 py-8">
    <h1 class="text-2xl font-bold text-[#0D1120] mb-6"><%= t('edit_commitment') %></h1>
    <%= render "form" %>
  </div>
</div>
